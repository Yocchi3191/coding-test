#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(int argc, char *argv[])
{
    int n, // カード枚数
        k; // 和
    cin >> n >> k;
    vector<int> cards(n); // カード

    // カード数値の取得
    for (int i = 0; i < n; i++)
    {
        cin >> cards[i];
    }

    int count; // 和がkになる組み合わせが出てきたときに増える
   for()

    cout << count;
}
